In the given question it was assumed that there is no connection between the nodes whose bandwidth is mentioned as infinite(∞) as nothing in practical life have infinite bandwidth

Thus, in the input file the numerical value of -1 was used to denote the infinite(∞) bandwidth.